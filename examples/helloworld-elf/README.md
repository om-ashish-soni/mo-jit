# Example — hello-world ELF

A statically linked ARM64 Linux hello-world, runnable under `mojit-run`.
Proves that the gum block cache, the svc dispatcher, and the minimal
file-open path work end to end.

Lands with M5.
